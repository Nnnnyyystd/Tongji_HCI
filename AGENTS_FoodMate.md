# FoodMate 项目开发控制文件（AGENTS.md）

> 本文件用于本地开发阶段约束整体产品风格、交互原则、代码实现边界和人机交互要求。  
> 本地 Agent、组员或开发者在修改项目代码前，应先阅读本文件，并在实现过程中持续遵守。  
> 项目目标不是做一个复杂健康管理 App，而是做一个**面向大学生的轻量化智能饮食记录 Agent Demo**。

---

## 1. 项目定位

### 1.1 项目名称

**FoodMate：面向大学生的智能饮食记录 Agent**

### 1.2 一句话介绍

FoodMate 通过文字、图片、语音等低负担输入方式，帮助大学生记录每日饮食，并通过 AI 识别、用户确认、饮食总结和趋势可视化，帮助用户更轻松地了解自己的饮食习惯。

### 1.3 项目本质

本项目是人机交互课程大作业中的**交互式 Web Demo**，不是完整商业 App，也不是医疗健康产品。

它应当重点展示：

- 用户如何低负担完成饮食记录；
- 系统如何及时反馈当前状态；
- AI 如何辅助而不是替代用户决策；
- 用户如何修改、撤销和控制 AI 结果；
- 系统如何用卡片、日历、趋势图降低用户理解成本；
- 整个交互流程如何体现人机交互设计原则。

---

## 2. 核心用户与使用场景

### 2.1 目标用户

目标用户为：

> 想了解自己饮食习惯，但不愿使用复杂健康管理软件的大学生。

### 2.2 用户特点

开发时应始终假设用户具有以下特点：

- 不想花很多时间记录饮食；
- 不愿填写复杂表格；
- 更习惯聊天式、卡片式、按钮式操作；
- 可能在食堂、宿舍、路上快速记录；
- 可能记不清具体克数和营养成分；
- 不希望被系统制造饮食焦虑；
- 希望得到简单、温和、可理解的反馈。

### 2.3 核心痛点

项目应围绕以下痛点设计：

1. 饮食记录输入成本高；
2. 拍照、文字、历史记录分散；
3. 用户不知道自己长期饮食习惯如何；
4. 传统健康 App 信息过重，容易带来压力；
5. AI 识别可能出错，需要用户确认和修改；
6. 用户需要清晰知道系统正在做什么、为什么给出建议。

---

## 3. 技术栈约束

本项目采用轻量化前后端分离架构。

### 3.1 前端

推荐技术：

- HTML
- CSS
- JavaScript
- Vite
- 可选：Chart.js 用于简单图表

前端目标：

- 实现手机端风格 Web Demo；
- 页面固定手机尺寸展示；
- 支持页面跳转、输入、确认、修改、删除、查看趋势；
- 不追求复杂动画，不引入过重框架。

### 3.2 后端

推荐技术：

- Python
- FastAPI
- SQLite

后端目标：

- 提供饮食记录接口；
- 提供 AI 识别模拟接口；
- 提供今日总结和周趋势接口；
- 提供用户偏好接口；
- 不做复杂权限系统，不做真实生产级部署。

### 3.3 AI Agent

AI Agent 允许采用：

- 规则解析；
- Mock 数据；
- 简单关键词匹配；
- 固定模板生成；
- 可选接入大模型 API，但不是必须。

重要原则：

> AI Agent 的作用是展示“交互逻辑”和“辅助反馈”，不是追求真实营养分析准确率。

---

## 4. 项目功能边界

### 4.1 必做功能

必须优先实现以下主流程：

1. 首页查看今日饮食；
2. 点击“记录一餐”；
3. 用户通过自然语言输入饮食内容；
4. 系统显示“AI 正在识别”状态；
5. 系统展示识别结果；
6. 用户确认或修改识别结果；
7. 保存记录；
8. 返回首页并更新今日饮食卡片；
9. 查看今日总结；
10. 查看每周趋势。

### 4.2 可选功能

以下功能可作为加分项：

- 图片上传模拟识别；
- 语音输入模拟；
- 饮食日历；
- 历史记录编辑；
- 偏好设置；
- 周报导出；
- AI 建议解释；
- 删除撤销；
- 常吃食物快捷记录。

### 4.3 禁止过度扩展

不要把项目扩展成以下方向：

- 医疗诊断系统；
- 减肥监督系统；
- 严格热量计算系统；
- 体重管理系统；
- 健身教练系统；
- 真实营养评估系统；
- 需要复杂登录权限和多用户后台的系统。

---

## 5. 人机交互总原则

开发过程中，所有功能都应优先满足以下 HCI 原则。

### 5.1 系统状态可见

用户进行任何操作后，系统都必须给出明确反馈。

必须包含：

- 输入后显示“正在识别”；
- 保存后显示“记录成功”；
- 删除后显示“已删除，可撤销”；
- 加载失败时显示原因；
- 空状态时给出下一步引导。

不要出现：

- 点击按钮没有任何反应；
- 页面突然跳转但没有说明；
- 按钮置灰但不解释原因；
- AI 处理时间过长但没有加载提示。

### 5.2 用户控制与自由

AI 不应直接替用户保存结果。  
所有 AI 识别结果必须经过用户确认。

必须支持：

- 修改食物名称；
- 修改餐次；
- 删除错误识别项；
- 添加漏识别食物；
- 保存后再次编辑；
- 删除后短时间内撤销。

### 5.3 防错与容错

系统应尽量防止用户出错，并帮助用户从错误中恢复。

必须处理：

- 空输入；
- 无法识别；
- 图片未上传；
- 食物识别不确定；
- 网络请求失败；
- 删除误操作；
- 表单信息不完整。

错误提示必须使用用户能理解的自然语言。

不要显示：

- “Error 500”
- “undefined”
- “null”
- “parse failed”
- “bad request”

应改为：

- “暂时没有识别到食物，请换一种说法试试。”
- “这条记录还没有保存，请先确认。”
- “网络连接失败，请稍后重试。”

### 5.4 减少记忆负担

用户不应该记住复杂规则。  
系统应将可选项、历史记录和下一步操作直接展示出来。

推荐做法：

- 用按钮选择餐次；
- 用卡片展示三餐；
- 用日历展示历史；
- 用标签展示食物类别；
- 用提示文案说明下一步；
- 用默认选项减少输入。

### 5.5 简洁与重点突出

页面只展示当前任务需要的信息。

避免：

- 一个页面堆太多模块；
- 同时展示大量营养术语；
- 过多颜色和按钮；
- 过长文字；
- 复杂表格；
- 需要用户横向滚动。

推荐：

- 卡片式布局；
- 大按钮；
- 明确标题；
- 2 到 3 个主要操作；
- 重要信息优先展示；
- 次要内容折叠或放到详情页。

### 5.6 贴近用户语言

系统文案应使用大学生日常能理解的表达。

推荐表达：

- “记录一餐”
- “今天吃了什么？”
- “我识别到这些食物，请确认”
- “今天蔬菜记录较少”
- “明天可以考虑加一份青菜”
- “为什么这样建议？”

避免表达：

- “营养摄入异常”
- “热量超标”
- “膳食结构不合格”
- “请调整代谢摄入比例”
- “您的饮食风险较高”

---

## 6. 非医疗化与非焦虑化原则

这是本项目最重要的产品边界之一。

### 6.1 系统不做医疗判断

禁止提供：

- 疾病诊断；
- 治疗建议；
- 严格减重计划；
- 强制节食建议；
- 对身体形态的评价；
- 对体重的判断；
- 任何可能制造焦虑的评价。

### 6.2 建议应温和

推荐文案：

- “今天蔬菜记录较少，明天可以考虑增加一份青菜。”
- “本周早餐记录比较稳定，可以继续保持。”
- “如果晚上容易忘记记录，可以开启睡前提醒。”
- “今天记录中水果较少，下次可以试试加一份水果。”

避免文案：

- “你今天吃太多了。”
- “你的饮食不健康。”
- “你必须减少主食。”
- “你的饮食很差。”
- “你需要控制自己。”

### 6.3 AI 结论必须弱化确定性

推荐使用：

- “根据当前记录”
- “大致来看”
- “可能”
- “可以考虑”
- “记录中显示”

避免使用：

- “你一定”
- “你必须”
- “系统判断”
- “医学上说明”
- “严重不足”

---

## 7. 核心页面规范

### 7.1 首页 / 今日饮食页

首页应回答三个问题：

1. 今天记录了什么？
2. 还有什么没记录？
3. 下一步可以做什么？

应包含：

- 今日日期；
- 早餐、午餐、晚餐、加餐卡片；
- 今日 AI 小结；
- “记录一餐”主按钮；
- 底部导航。

交互要求：

- 未记录餐次显示空状态；
- 点击餐次卡片可查看详情；
- 记录成功后首页应立即更新。

### 7.2 记录一餐页

应提供低负担输入方式：

- 文字输入；
- 图片上传模拟；
- 语音输入模拟；
- 常吃食物快捷选择。

主文案建议：

> 今天吃了什么？可以直接用一句话告诉我。

按钮文案：

- “开始识别”
- “清空”
- “返回首页”

### 7.3 AI 识别确认页

这是用户控制权的核心页面。

必须包含：

- 识别出的餐次；
- 食物列表；
- 可编辑数量；
- 可删除按钮；
- 添加食物按钮；
- 确认保存按钮。

重要原则：

> AI 识别结果不能直接入库，必须用户确认后保存。

### 7.4 今日总结页

应包含：

- 三餐完整度；
- 饮食结构概览；
- 温和建议；
- 建议解释按钮。

示例：

> 根据今天的记录，主食和蛋白质记录较完整，蔬菜类食物较少。明天可以考虑增加一份绿叶菜。

### 7.5 饮食日历页

应包含：

- 月视图；
- 每日记录状态；
- 点击日期查看详情；
- 空日期提示。

状态示例：

- 完整记录；
- 部分记录；
- 未记录。

### 7.6 每周趋势页

应包含：

- 本周记录天数；
- 早餐记录次数；
- 蔬菜记录天数；
- 水果记录天数；
- 常见食物 Top 5；
- 本周温和建议。

注意：

> 趋势图的目标是帮助用户理解习惯，不是评价用户好坏。

---

## 8. 推荐交互主流程

必须优先保证以下流程稳定：

```text
首页
→ 点击“记录一餐”
→ 输入“今天中午吃了鸡腿饭和酸奶”
→ 点击“开始识别”
→ 显示“正在识别”
→ 展示 AI 识别结果
→ 用户确认或修改
→ 点击“确认保存”
→ 显示“记录成功”
→ 返回首页
→ 首页午餐卡片更新
→ 查看今日总结
→ 查看每周趋势
```

这个流程是课堂汇报的核心演示路径。  
任何新功能都不能破坏这个主流程。

---

## 9. API 设计规范

### 9.1 统一返回格式

后端接口应尽量使用统一格式：

```json
{
  "success": true,
  "message": "操作成功",
  "data": {}
}
```

失败时：

```json
{
  "success": false,
  "message": "请先输入饮食内容",
  "data": null
}
```

### 9.2 核心接口

建议至少包含：

```text
POST /api/agent/parse
POST /api/meals
GET  /api/meals/today
GET  /api/meals?date=YYYY-MM-DD
PUT  /api/meals/{id}
DELETE /api/meals/{id}
GET  /api/agent/today-summary
GET  /api/stats/week
GET  /api/preferences
PUT  /api/preferences
```

### 9.3 AI 解析接口示例

请求：

```json
{
  "text": "今天中午吃了鸡腿饭和酸奶"
}
```

返回：

```json
{
  "success": true,
  "message": "识别完成，请确认结果",
  "data": {
    "meal_type": "午餐",
    "foods": [
      {
        "food_name": "鸡腿饭",
        "amount": "1份",
        "category": "主食/蛋白质"
      },
      {
        "food_name": "酸奶",
        "amount": "1杯",
        "category": "蛋白质/饮品"
      }
    ],
    "agent_message": "我识别到你午餐吃了鸡腿饭和酸奶，请确认是否保存。"
  }
}
```

---

## 10. 数据库建议

### 10.1 meals 表

```text
id
meal_type
meal_date
raw_input
note
created_at
updated_at
```

### 10.2 foods 表

```text
id
meal_id
food_name
amount
category
created_at
```

### 10.3 preferences 表

```text
id
goal
taste
reminder_time
avoid_foods
created_at
updated_at
```

---

## 11. AI Agent 规则建议

### 11.1 餐次识别

```text
包含“早上”“早餐”“早饭” → 早餐
包含“中午”“午餐”“午饭” → 午餐
包含“晚上”“晚餐”“晚饭” → 晚餐
否则 → 加餐或待确认
```

### 11.2 食物类别识别

```text
米饭、面条、馒头、包子、粥 → 主食
鸡蛋、牛奶、酸奶、鸡肉、牛肉、鱼 → 蛋白质
青菜、白菜、菠菜、西兰花、黄瓜 → 蔬菜
苹果、香蕉、橙子、葡萄 → 水果
奶茶、咖啡、豆浆、饮料 → 饮品
其他 → 其他
```

### 11.3 总结生成规则

如果蔬菜数量为 0：

> 今天记录中蔬菜类食物较少，明天可以考虑增加一份青菜。

如果早餐未记录：

> 今天还没有早餐记录，如果你确实吃了早餐，可以补充一下。

如果三餐都有记录：

> 今天三餐记录比较完整，可以继续保持这个记录习惯。

---

## 12. UI 风格规范

### 12.1 整体风格

关键词：

- 清爽；
- 温和；
- 轻量；
- 低压力；
- 卡片化；
- 移动端友好。

### 12.2 颜色建议

可以使用以下风格：

```text
背景色：#F8FAF7
主色：#5BAA75
辅助色：#F4B860
文字主色：#263238
文字次色：#7A7A7A
卡片背景：#FFFFFF
错误提示：#D9534F
```

### 12.3 布局建议

- 手机外框宽度建议 390px；
- 页面内容使用 16px 左右边距；
- 卡片圆角 16px；
- 主按钮高度不少于 44px；
- 重要按钮放在拇指容易点击的位置；
- 底部导航保持一致。

### 12.4 字体层级

```text
页面标题：20-24px
卡片标题：16-18px
正文：14-16px
辅助说明：12-14px
```

---

## 13. 可用性测试要求

开发时应为以下测试任务提供完整路径。

### 13.1 测试任务

任务 1：

> 记录一顿午餐：“今天中午吃了鸡腿饭和酸奶”。

任务 2：

> 修改 AI 识别结果，例如将“牛奶”改为“酸奶”。

任务 3：

> 查看本周蔬菜记录情况。

任务 4：

> 删除一条记录并撤销。

### 13.2 测试指标

记录以下指标：

- 是否完成任务；
- 完成时间；
- 点击次数；
- 是否需要帮助；
- 是否理解 AI 反馈；
- 是否能找到修改入口；
- 是否觉得建议有压力；
- 满意度评分。

---

## 14. 代码开发原则

### 14.1 优先保证主流程

任何代码修改都应优先保证主流程可运行。

不要为了实现次要功能破坏：

- 首页；
- 记录一餐；
- AI 识别；
- 用户确认；
- 保存；
- 总结；
- 趋势。

### 14.2 模块化开发

建议模块划分：

```text
框架与公共组件模块
饮食记录与 AI 识别模块
饮食日历与历史记录模块
饮食总结与趋势分析模块
```

### 14.3 命名规范

前端页面命名：

```text
home.js
record.js
confirm.js
calendar.js
trend.js
summary.js
settings.js
```

后端文件命名：

```text
meals.py
agent.py
stats.py
preferences.py
food_parser.py
summary_agent.py
```

### 14.4 提交前检查

每次提交前应确认：

- 前端可以启动；
- 后端可以启动；
- 主流程可以运行；
- 没有明显控制台报错；
- 空输入有提示；
- 保存成功有反馈；
- 删除可恢复或有确认；
- 文案没有医疗化、焦虑化表达。

---

## 15. 本地 Agent 开发指令

如果使用本地 Agent 辅助开发，应遵守以下指令：

1. 修改代码前先阅读本文件；
2. 优先保证人机交互体验，而不是堆功能；
3. 不要引入过重依赖；
4. 不要将项目改成真实医疗健康 App；
5. 不要生成制造焦虑的文案；
6. 所有 AI 结果都应允许用户确认和修改；
7. 所有用户操作都应有反馈；
8. 所有错误都应有可理解提示；
9. 新增页面应保持手机端风格一致；
10. 新增功能应服务于“低负担记录饮食、理解饮食习惯”的目标。

---

## 16. 最终验收清单

在汇报前，项目至少应满足：

- [ ] 浏览器中能显示手机界面；
- [ ] 首页能展示今日饮食；
- [ ] 用户能记录一餐；
- [ ] AI 能返回模拟识别结果；
- [ ] 用户能确认或修改识别结果；
- [ ] 保存后首页能更新；
- [ ] 能查看今日总结；
- [ ] 能查看每周趋势；
- [ ] 空输入有提示；
- [ ] 保存和删除有反馈；
- [ ] 文案温和、不制造焦虑；
- [ ] 页面风格统一；
- [ ] 能支持课堂演示的完整流程。

---

## 17. 项目汇报时的核心表达

汇报时应强调：

> FoodMate 不是一个传统饮食统计工具，而是一个面向大学生日常生活的智能饮食记录 Agent。它通过低负担输入、AI 辅助识别、用户确认机制、温和反馈和趋势可视化，体现了以用户为中心的人机交互设计思想。

核心 HCI 亮点包括：

1. 低输入负担；
2. 系统状态可见；
3. AI 结果可确认；
4. 错误可恢复；
5. 信息可视化；
6. 非医疗、非焦虑化；
7. 用户控制权优先；
8. 简洁一致的移动端界面。

---

## 18. 最重要的开发原则

> 不要只问“这个功能能不能做”，而要先问：  
> **这个功能是否让用户更容易完成任务？是否降低了输入成本？是否给出了清晰反馈？是否让用户保持控制权？是否避免了不必要的压力？**

如果答案是否定的，就不要优先实现这个功能。
