# FoodMate 上手指南

## 目录

1. [环境准备](#1-环境准备)
2. [启动项目](#2-启动项目)
3. [注册与登录](#3-注册与登录)
4. [记录一餐](#4-记录一餐)
5. [饮食日历](#5-饮食日历)
6. [趋势分析](#6-趋势分析)
7. [偏好设置](#7-偏好设置)
8. [常见问题](#8-常见问题)

---

## 1. 环境准备

### 需要提前安装

| 工具 | 用途 | 下载 |
|---|---|---|
| Anaconda / Miniconda | 管理 Python 环境 | https://www.anaconda.com |
| Node.js 18+ | 运行前端 | https://nodejs.org |

### API Key 配置

项目根目录有一个 `.env` 文件，填入你的 API Key：

```
DEEPSEEK_API_KEY=sk-xxxxxxxx   # 用于 AI 周总结
QWEN_API_KEY=sk-xxxxxxxx       # 用于拍照识别与评分
```

- **DeepSeek Key** 申请地址：https://platform.deepseek.com
- **通义千问 Key** 申请地址：https://bailian.console.aliyun.com（新账号有免费额度）

### 一键安装依赖（首次使用）

在项目根目录打开终端，执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\setup_foodmate_env.ps1
```

或者（cmd 环境）：

```cmd
setup_foodmate_env.bat
```

脚本会自动创建名为 `foodmate` 的 conda 环境并安装所有依赖。

---

## 2. 启动项目

每次使用都需要同时启动后端和前端，**开两个终端窗口**。

### 终端 1：启动后端

```cmd
conda activate foodmate
uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000
```

看到 `Application startup complete.` 表示后端就绪。

### 终端 2：启动前端

```cmd
cd frontend
npm install
npm run dev
```

### 打开浏览器

访问 http://127.0.0.1:5173，页面加载后会自动跳转到登录页。

> **注意**：后端和前端必须同时运行，关掉任何一个另一个也会无法正常工作。

---

## 3. 注册与登录

### 首次使用：注册账号

1. 打开页面后看到登录界面，点击底部「**还没有账号？去注册**」
2. 填写以下信息：
   - **用户名**：只能包含中文、英文、数字或下划线，至少 2 个字符
   - **昵称**：显示在首页的名字，可以随意填写（不填默认用用户名）
   - **密码**：至少 6 位
   - **确认密码**：再输入一遍
3. 点击「**注册并进入**」，成功后自动跳转到首页

### 之后使用：直接登录

1. 输入注册时的用户名和密码
2. 点击「**登录**」

### 退出登录

进入「**设置**」页面，点击右上角「**退出**」按钮。

---

## 4. 记录一餐

这是 FoodMate 的核心功能。点击底部导航栏的「**记录**」进入。

### 完整流程

```
选择日期和餐次 → （可选）上传照片 → 填写食物描述 → AI 识别评分 → 保存
```

### 详细步骤

**Step 1：选择日期和餐次**
- 日期默认为今天，可以手动修改
- 餐次选择：早餐 / 午餐 / 晚餐 / 零食

**Step 2：上传照片（可选）**
- 点击虚线框区域，从相册或文件中选择食物照片
- 照片选择后会在框内预览
- 照片会连同文字描述一起发给 AI 分析

**Step 3：填写食物描述**
- 在文本框中输入吃了什么，例如：`米饭、番茄炒蛋、青菜、豆腐汤`
- 如果只上传了照片，文字描述可以不填（AI 会直接识别图片）
- 如果没有照片，文字描述**必填**

**Step 4：AI 识别 & 评分**
- 点击「**AI 识别 & 评分**」按钮
- 等待 3–10 秒，AI 会返回：
  - 识别出的食物列表（自动填入描述框，可手动修改）
  - **综合评分**（0–100 分）
  - 三项分值：食材多样性（35分）、营养均衡性（35分）、烹饪健康度（30分）
  - 一句话评价

**Step 5：保存**
- 确认描述无误后，点击「**保存记录**」
- 保存成功后自动跳回首页，可以看到刚才记录的这餐

> **提示**：AI 识别不准确时，可以直接编辑文字描述框里的内容再保存。评分和评价会一起存档，之后在日历页面可以查看。

---

## 5. 饮食日历

点击底部导航栏的「**日历**」进入。

### 页面说明

- 上方显示当月日历，**今天**用绿色实心标记
- 有饮食记录的日期会显示绿色边框
- 下方显示选中日期的餐食详情

### 查看历史记录

1. 点击日历上任意一天
2. 下方详情区域更新为该日的所有餐食
3. 每条餐食显示：餐次 + 食物内容

### 查看 AI 评分详情

如果某餐做过 AI 识别评分，餐食旁边会显示绿色分数标签（如 **78分**）：

1. 点击分数旁边的「**详情**」按钮
2. 展开显示：
   - AI 的一句话评价
   - 三项分值细则（食材多样 / 营养均衡 / 烹饪健康）
3. 再次点击「**收起**」可以折叠

---

## 6. 趋势分析

点击底部导航栏的「**趋势**」进入。

### 本周评分折线图

- 展示过去 7 天每天的**日均评分**（0–100 分）
- 当天没有带评分的记录时，该天不显示数据点（图表出现空缺属于正常）
- 随着每天记录并进行 AI 评分，图表会逐渐填满

### AI 饮食分析

1. 进入趋势页面，如果之前生成过分析，会自动显示上次的内容（右上角标注生成日期）
2. 点击「**生成本周分析**」或「**重新生成分析**」
3. 等待 5–15 秒，AI 会根据本周所有记录生成一段总结：
   - 本周饮食亮点
   - 需要改进的地方
   - 2 条具体建议
4. 分析结果**自动保存**，下次打开趋势页时直接展示，无需重新生成

> **提示**：记录越多、评分的餐食越多，AI 分析的参考信息越丰富，总结质量越高。

---

## 7. 偏好设置

点击右上角**齿轮图标**进入设置页面。

### 可配置项

| 设置项 | 说明 | 示例 |
|---|---|---|
| 饮食目标 | 记录饮食的主要目的 | 了解饮食习惯 / 增加蔬菜摄入 |
| 口味偏好 | 个人口味描述 | 清淡、少辣 |
| 提醒时间 | 每日提醒记录的时间 | 19:30 |
| 忌口食物 | 不能吃或不喜欢的食物 | 花生、香菜 |

修改后点击「**保存设置**」，数据同步到后端。

---

## 8. 常见问题

**Q：页面显示"加载失败"**
> 检查后端终端是否正在运行，确认没有报错。重新执行 `conda activate foodmate` 和 `uvicorn` 命令。

**Q：删除数据库后重新开始**
> 在项目根目录执行 `del backend\data\foodmate.db`（cmd）或重新注册账号即可。

**Q：AI 识别报错**
> 检查 `.env` 文件中的 API Key 是否正确填写，确认没有多余空格。通义千问 Key 对应 `QWEN_API_KEY`。

**Q：评分总是很低**
> AI 根据描述的食物内容评分，描述越具体（食材种类多、包含蔬菜蛋白质主食）分数越高；油炸食品、单一食材得分较低，这是正常的。

**Q：日历上某天有记录但没有绿色边框**
> 绿色边框标记的是通过「记录」功能保存的餐食，手动写入或测试数据不在此列。

---

*FoodMate — 面向大学生的轻量化饮食记录 Demo*
